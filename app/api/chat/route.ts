import { NextResponse } from "next/server";

// Placeholder grounded-chat endpoint. The Grok prompt, five-section format,
// and Mock_Fallback are implemented in task 18.
export async function POST() {
  return NextResponse.json({ status: "chat endpoint not yet implemented" });
}
