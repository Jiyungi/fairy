// Placeholder entrypoint so `next build` has a page to compile.
// Real UI is produced through the Impeccable skill (see design.md).
export default function Home() {
  return (
    <main>
      <h1>Fairy</h1>
      <p>Scaffold ready.</p>
    </main>
  );
}
